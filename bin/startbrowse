#!/usr/bin/env sh

session="browse"
tmux has-session -t $session 2>/dev/null
# start session if it does not currently exist
if [ "$?" -eq 1 ] ; then
  tmux new-session -d -s $session
fi

tmux attach-session -t $session